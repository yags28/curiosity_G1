# CDL Project — Core requirements (pinned at install date May 2026)
# Install with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# Isaac Lab & Isaac Sim are installed separately — see deps/ folder:
#   pip install -e deps/IsaacLab/source/isaaclab
#   pip install -e deps/IsaacLab/source/isaaclab_assets
#   pip install -e deps/IsaacLab/source/isaaclab_tasks
#   pip install -e deps/IsaacLab/source/isaaclab_mimic
#   pip install isaacsim==4.5.0.0 isaacsim-rl==4.5.0.0 isaacsim-replicator==4.5.0.0 \
#               isaacsim-extscache-physics==4.5.0.0 isaacsim-extscache-kit==4.5.0.0 \
#               isaacsim-extscache-kit-sdk==4.5.0.0 --extra-index-url https://pypi.nvidia.com
#
# rsl_rl:
#   pip install -e deps/rsl_rl
#
# unitree_rl_gym:
#   DO NOT pip install — depends on deprecated isaacgym. URDF assets are used
#   directly by path from deps/unitree_rl_gym/resources/robots/g1_description/

# ── PyTorch (GPU — installed separately with CUDA wheel) ──────────────────────
# torch==2.12.0+cu130
# torchvision==0.27.0+cu130
# torchaudio==2.11.0+cu130

# ── Scientific Python ─────────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.15.3
matplotlib==3.10.9
seaborn==0.13.2
pandas==2.3.3
scikit-learn==1.7.2
opencv-python==4.11.0.86
h5py==3.16.0
imageio==2.37.3
imageio-ffmpeg==0.6.0
einops==0.8.2

# ── RL / Gymnasium ────────────────────────────────────────────────────────────
gymnasium==1.2.1
stable-baselines3==2.8.0

# ── Simulation ────────────────────────────────────────────────────────────────
mujoco==3.8.1
dm_control==1.0.41

# ── Robotics & Geometry ───────────────────────────────────────────────────────
pin==2.7.0
trimesh==4.12.2
yourdfpy==0.0.60
transforms3d==0.4.2
open3d==0.19.0
roboticstoolbox-python==1.1.1

# ── Config & Experiment Tracking ─────────────────────────────────────────────
PyYAML==6.0.3
omegaconf==2.3.0
hydra-core==1.3.2
wandb==0.27.0
tensorboard==2.20.0
tqdm==4.67.3

# ── TensorRT (inference optimisation) ────────────────────────────────────────
# tensorrt==10.16.1.11  (installed via: pip install tensorrt --extra-index-url https://pypi.nvidia.com)

# ── Dev Tools ─────────────────────────────────────────────────────────────────
black==26.5.1
flake8==7.3.0
flake8-bugbear==25.11.29
isort==8.0.1
autopep8==2.3.2
pytest==9.0.3
pytest-cov==7.1.0
pytest-mock==3.15.1
pre-commit==4.6.0
ipython==8.39.0
jupyter==1.1.1

# ── Misc Utilities ────────────────────────────────────────────────────────────
rich==15.0.0
networkx==3.4.2
sympy==1.14.0
Pillow==11.3.0
requests==2.34.2
